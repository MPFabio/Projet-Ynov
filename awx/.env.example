# AWX est déployé via awx-without-k8s (Ansible + Docker).
# Le mot de passe admin et les secrets sont dans le demo du dépôt awx-without-k8s
# (group_vars / host_vars). Ce fichier .env n'est pas utilisé par le déploiement actuel.
